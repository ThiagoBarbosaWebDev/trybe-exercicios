const array = ['HTML', 'javascript', 'css', 'react', 'redux'];

let teste = "tryber x aqui"

const replateLetter = (str, newStr) => str.replace('x',newStr)

console.log(replateLetter(teste, 'bebeto'))